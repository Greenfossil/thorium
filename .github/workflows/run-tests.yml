name: Run tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    uses: Greenfossil/.github/.github/workflows/run-tests.yml@main
    secrets: inherit
      
